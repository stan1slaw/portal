<h1 class="text-center"><%= t('profile.name') %></h1>
<div class="information_profile text-center">
<% if @user.privat == true %>
<p><%= t('profile.private') %></p>
<% else %>
    <%= render partial: "films/information", locals: {current_user:@user} %>
<% end %>

</div>